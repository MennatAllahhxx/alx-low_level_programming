#include "main.h"
/**
 * print_rev - a fun to print reverse
 * @s: string to be printed
 * Return: nth
 */
void print_rev(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
}
while (i >= 0)
{
i--;
_putchar(s[i]);
}
_putchar('\n');
}
