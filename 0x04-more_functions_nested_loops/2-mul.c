#include "main.h"
/**
 * mul - a function to find multiplication of two numbers
 * @a: first number
 * @b: second number
 * Return: result
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
